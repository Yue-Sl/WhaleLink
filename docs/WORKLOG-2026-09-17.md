# WhaleLink 工作日志追加记录（2026-09-17）

本文件是 `WORKLOG.md` 在文件系统刷新故障期间的不可变追加记录；恢复后应将其内容合并回
主日志，不得丢失任务状态或验证证据。

## WL-0001 — 初始化工程追溯体系

- **状态：** PASS
- **改动：** 建立 Git `main` 仓库、Rust workspace、Avalonia 项目、部署样例、CI、规格、ADR、验证目录和发行检查清单。
- **关键命令：** 工作区/工具链检查；`git init --initial-branch=main`。
- **验证证据：** `verification/m0-environment.md`、`verification/m1-m2-build.md`。
- **下一步：** 实现各 M0--M4 发行门禁。

## WL-0002 — M0 EasyTier 版本锁定

- **状态：** PARTIAL
- **改动：** 锁定官方 v2.6.4 Windows/Linux x86_64 资产及 SHA-256，新增 ADR 0001 和第三方声明。
- **关键命令：** GitHub Releases latest API；尝试下载 Windows 资产。
- **验证证据：** `verification/m0-easytier-metadata.md`。
- **风险/阻塞：** 资产下载被远端重置，未留下本地文件；真实双节点验证未运行。
- **下一步：** 在隔离的 Windows+Linux 环境完成下载校验与组网矩阵。

## WL-0003 — M1 Core、CLI 与本地协议骨架

- **状态：** PARTIAL
- **改动：** 新增 Core、协议、daemon、CLI；实现 TOML schema 校验、状态接口、受管进程接口、打码、IPC v1 契约和 CLI 配置验证。
- **关键命令：** `cargo fmt --all -- --check`；`cargo test --workspace`；CLI `validate-config`。
- **验证证据：** `verification/m1-m2-build.md`。
- **风险/阻塞：** Windows Named Pipe 服务端 ACL、DPAPI 和跨平台原子替换仍未实现。

## WL-0004 — M2 控制面 API 与邀请码状态机

- **状态：** PARTIAL
- **改动：** 新增 Axum Server、`/api/v1`、令牌鉴权、短期单次可撤销邀请、兑换成员登记和凭据 epoch 轮换。
- **关键命令：** `cargo test --workspace`；`cargo clippy --workspace --all-targets -- -D warnings`。
- **验证证据：** `verification/m1-m2-build.md`。
- **风险/阻塞：** 状态只存在于内存；持久化、TLS/反向代理、限流和审计未实现。

## WL-0005 — M3 桌面应用骨架

- **状态：** PARTIAL
- **改动：** 新增 Avalonia 启动、状态页和 Named Pipe 客户端，使用 `asInvoker`。
- **关键命令：** `dotnet build apps/WhaleLink.Desktop/WhaleLink.Desktop.csproj --configuration Release`。
- **验证证据：** `verification/m1-m2-build.md`。
- **风险/阻塞：** Pipe server 和管理/加入完整界面未实现。

## WL-0006 — M4 发行与合规骨架

- **状态：** PLANNED
- **改动：** 新增 Dockerfile、Compose、systemd unit、GitHub Actions、第三方声明和发行检查清单。
- **验证证据：** `verification/release-checklist.md`。
- **风险/阻塞：** 安装包、SBOM、Linux 构建与干净环境验收尚未执行。
